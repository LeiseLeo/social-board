# ImageBoard

## Start development

```bash
   # Use docker container for the python preprocessor
    docker compose -f docker/docker-compose.dev.yml up --build
```

```bash
    # use the ng serve development environment from angular
    ng serve
```

## Production setup
